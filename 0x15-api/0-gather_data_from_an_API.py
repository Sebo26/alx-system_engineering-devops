#!/usr/bin/python3
import requests
employee ID must be an integer
requests.get(EMPLOYEE_NAME, NUMBER_OF_DONE_TASKS, TOTAL_NUMBER_OF_TASKS)
requests.post(f"Employee {EMPLOYEE_NAME} is done with tasks{NUMBER_OF_DONE_TASKS}/{TOTAL_NUMBER_OF_TASKS}: ")
requests.get(TASK_TITLE)
requests.post({TASK_TITLE})
